[project]
name = "ai-core"
version = "0.1.0"
description = "共享核心模块"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
