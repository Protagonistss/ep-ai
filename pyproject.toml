[project]
name = "ep-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "crewai>=1.12.1",
    "lancedb>=0.30.0,<0.30.1",
    "python-dotenv>=1.1.1",
]
